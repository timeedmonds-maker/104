name: Fetch Rockets on-off

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fetch-rockets-onoff.yml'
      - 'scripts/fetch_rockets_onoff.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests curl-cffi pandas
      - run: python scripts/fetch_rockets_onoff.py
      - name: Commit results
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add data/
          git diff --cached --quiet || git commit -m "Add official NBA Rockets on-off data [skip ci]"
          git push
