name: TREB v14 full build - macOS ARM race

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, ready_for_review]

concurrency:
  group: treb-v14-macos-arm-race
  cancel-in-progress: true

jobs:
  build:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - run: echo 'Retired after Stage1 completion; Stage2 now uses slim checkpoint bypass.'
