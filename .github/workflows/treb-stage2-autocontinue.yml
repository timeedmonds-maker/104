name: TREB Stage2 autonomous continuation

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo 'Legacy Stage2 autonomous continuation retired during solved-engine recovery. No automatic dispatch.'
