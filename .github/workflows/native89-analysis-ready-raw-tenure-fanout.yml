name: Native89 analysis-ready raw tenure fanout (superseded)
on:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: native89-analysis-ready-raw-tenure-fanout
  cancel-in-progress: true
jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by native89-analysis-ready-raw-tenure-trackable.yml (256 smaller checkpointed shards).'
